```sh-session
$ yarn
$ yarn start
# Inspect either of the headings and see they have applied nth-child styling
$ yarn build
# Open up built app, and repeat the above. Notice that the nth-child styling is gone
```